[project]
name = "zigbee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "paho-mqtt>=2.1.0",
    "pyyaml>=6.0.3",
]

