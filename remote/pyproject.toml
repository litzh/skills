[project]
name = "remote"
version = "0.1.0"
description = "Broadlink IR remote control CLI tool"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "broadlink>=0.19.0",
    "tomli-w>=1.0.0",
]

[tool.uv]
package = false
